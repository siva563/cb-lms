# CodeBegun LMS (MERN, SaaS-ready)

Monorepo:
- apps/api  -> Node.js + Express + MongoDB
- apps/web  -> React + Bootstrap (Vite)

## Setup
1) Copy `.env.example` to `.env` and update values
2) Install deps: `npm install`
3) Run:
   - API: `npm run dev -w @cb/api`
   - Web: `npm run dev -w @cb/web`
